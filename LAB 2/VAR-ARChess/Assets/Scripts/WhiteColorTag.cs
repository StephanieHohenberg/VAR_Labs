using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhiteColorTag : MonoBehaviour
{
	public bool isWhite;

    // Start is called before the first frame update
    void Start()
    {
    	isWhite = true;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
