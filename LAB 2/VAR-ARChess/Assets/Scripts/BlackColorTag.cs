using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlackColorTag : MonoBehaviour
{
	public bool isWhite;

    // Start is called before the first frame update
    void Start()
    {
    	isWhite = false;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
