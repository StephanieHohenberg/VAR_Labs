using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChessLogic : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //while no chessboard has been set, use the AR Raycast to detect the real world plane

        //after initialisation use the regular unity raycast to access the virtual gameobjects in the screen to drag them around
    }

    //Input handler for raycasts of 

    // Chess 
}
